fn main() {
    let floored = 2 / 3;
    println!("result: {}", floored);
    //let division: f32 = 2 / 3; // mismatched types
    let division: f32 = 2.0 / 3.0;
    println!("result: {}", division);
}
